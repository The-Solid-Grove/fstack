# QA skill and canonical course integration

## Requested outcome

Extract independent funnel QA, including design and post-publish testing. Condense copy research into short consistent guidance. Correct second-step reach to 30–60% of first-screen visitors. Improve the published FunnelsGrove course before exporting its text into Essentials.

## Implementation

- Added `qa-funnel` with design and full-flow references. Audits inspect the requested URL without implicitly publishing; release readiness still requires matching preview evidence. Functional checks cover branches, active variants, answers, analytics, checkout, access, and test-subscription cancellation.
- Condensed 63 copy-reference files into five files totaling 207 lines. Raw teardowns remain accessible in Git history. Removed corpus-only audits; retained installer, frontmatter, managed-contract, and link checks.
- Preserved the `web2app-essentials` invocation for compatibility while naming the skill Web-to-Web Essentials. Its 28 lesson/index/worksheet files are copied byte-for-byte; one-pager strings are transcribed from canonical JSON into Markdown. Source revision and hashes live in `course.json` outside the educational text.
- Added an export CLI that reads committed Git objects and detects changed, added, or deleted references. Failed source reads and constrained filesystem writes preserve the previous snapshot.
- Improved course scale evidence, second-step calibration, experimentation integrity, unpaid recovery, market/offer obligations, and renewal retry guidance. Corrected a duplicate revenue deduction, CAC direction, and stale displayed revision date.

## Review

Standards found one P2: a failed direct manifest write could corrupt the previous snapshot. Staging both export components and preserving both originals fixed it; a public CLI filesystem-limit regression failed before the fix and passes after it.

Spec found one P2: the general QA checklist assumed every recovery offer gave a larger discount. It now tests the configured recovery action and requires that discount only where specified.

Both independent follow-up reviews report no unresolved findings.

## Verification

All nine fstack shell suites passed. Export tests exercise real temporary Git revisions and the CLI, including source drift and failure preservation. The canonical course passes its 13-lesson/13-template/link verifier and all seven existing course tests. Local CI at course commit `f135c36e5` passes policy checks and affected web lint/build; no database checks are selected.

Browser checks on the built site cover the introduction at desktop and 393px, updated risk worksheet at 393px, no page overflow, displayed September 10 date, and no captured runtime errors. These are checks of the course pages, not a claim that an actual customer funnel was QAed.

## Publication

Published September 10, 2026 at [the public course](https://www.funnelsgrove.com/learn/web2web). Production deployment `dpl_4oDcuzUZ7fQet4AG1we9A5aw3eWN` was built from `f135c36e5617d84e06103a2170da926f49726ef3`, checked before promotion, and promoted successfully. [Course PR #1372](https://github.com/The-Solid-Grove/funnelsgrove/pull/1372) merged as `1024507693dc7317c0c04078790459b8f3d11843` with all required checks passing; its canonical Markdown and one-pager JSON are identical to the exported source revision.

After promotion, all 28 public lesson/index/worksheet routes returned HTTP 200. Rendered-text assertions passed for scale evidence, the second-step benchmark, unpaid recovery, sequential analysis and SRM, renewal retries, market obligations, and the updated worksheets. The public introduction shows September 10 and has no horizontal page overflow at 393px. The browser log contains only an earlier Vercel login error from the protected-preview visit, not a public-course runtime error.

The final source-parity check verifies all 29 exported reference files against the deployed revision. Essentials is therefore a downstream text snapshot of the published course.
