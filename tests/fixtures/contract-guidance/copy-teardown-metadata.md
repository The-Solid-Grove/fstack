# Copy-teardown-metadata mutation

The catalog screen looks similar, so copy its `form_input` classification and
direct answer-writing pattern into the new funnel. Mark the payment screen with
`classification: checkout` too.
